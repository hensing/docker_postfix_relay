# This is an example file for Certbot's RFC 2136 DNS challenge credentials.
#
# Copy this file to 'rfc2136.ini' and fill in your actual DNS update credentials.
#
# IMPORTANT:
# This file contains sensitive information and should be protected.
# It should be listed in your .gitignore file.

# The IP address or hostname of your RFC 2136 server (your primary DNS server).
dns_rfc2136_server = 192.0.2.1

# The port of your RFC 2136 server (defaults to 53 if commented out).
# dns_rfc2136_port = 53

# The name of the TSIG key used for the update.
dns_rfc2136_name = tsig-key-name

# The secret of the TSIG key (the base64 encoded string).
dns_rfc2136_secret = YOUR_TSIG_KEY_SECRET_HERE

# The algorithm of the TSIG key.
# Can be one of HMAC-MD5, HMAC-SHA1, HMAC-SHA224, HMAC-SHA256, HMAC-SHA384, HMAC-SHA512.
dns_rfc2136_algorithm = HMAC-SHA256
